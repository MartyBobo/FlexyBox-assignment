@inherits LayoutComponentBase

<div class="page">    <header class="app-header">
        <div class="app-title">FlexyBox</div>
        <div class="app-nav">
            <a href="/" class="nav-icon">
                <img src="/images/icons/home-icon.svg" alt="Home" class="nav-icon-img" />
            </a>
        </div>
    </header>

    <main>
        <div class="content">
            @Body
        </div>
    </main>    <footer class="app-footer">
        <a href="/" class="footer-icon @(IsActive("/") ? "active" : "")">
            <img src="/images/icons/home-icon.svg" alt="Home" class="footer-icon-img" />
            <div>Home</div>
        </a>
        <a href="/search" class="footer-icon @(IsActive("/search") ? "active" : "")">
            <img src="/images/icons/search-icon.svg" alt="Search" class="footer-icon-img" />
            <div>Search</div>
        </a>
        <a href="/favorites" class="footer-icon @(IsActive("/favorites") ? "active" : "")">
            <img src="/images/icons/heart-icon.svg" alt="Favorites" class="footer-icon-img" />
            <div>Favorites</div>
        </a>
        <a href="/profile" class="footer-icon @(IsActive("/profile") ? "active" : "")">
            <img src="/images/icons/profile-icon.svg" alt="Profile" class="footer-icon-img" />
            <div>Profile</div>
        </a>
    </footer>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

@code {
    [Inject]
    private NavigationManager NavigationManager { get; set; }

    private bool IsActive(string href)
    {
        var currentUri = NavigationManager.Uri;
        var relativePath = NavigationManager.ToBaseRelativePath(currentUri);
        
        if (string.IsNullOrEmpty(relativePath) && href == "/")
            return true;
            
        return relativePath.StartsWith(href.TrimStart('/'));
    }
}